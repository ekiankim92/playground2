export interface StyledUICommonProps {
  $variant?: string;
  $size?: string;
  $colorPalette?: string;
  $style?: React.CSSProperties;
  children?: React.ReactNode;
}

export interface StyleProps {
  children?: React.ReactNode;
}
